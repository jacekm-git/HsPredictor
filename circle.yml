machine:
  ghc:
    version: 7.8.4
  services:
        - docker
test:
  cache_directories:
    - .cabal-sandbox/
  override:
    - sudo sh debian_sandbox
    - cabal sandbox init
    - cabal install --only-dependencies --enable-tests
    - cabal test --show-details=always --test-options="--color"
    - cabal build
